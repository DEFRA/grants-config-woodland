{
  "_comment": "Per-fixture expectations for the GAS integration suite. Key = filename in configurations/woodland/gas/test-data/. accepted:true => POST /applications returns 204 and emits a grant_application_created CloudEvent. accepted:false => returns 400; set errorContains to assert the validation message substring (and no event is emitted). NOTE: boundary-minimum-values.json and complex-eligibility-flags.json are currently REJECTED by the cross-field rules in gas.json (rules the raw JSON schema does not enforce) — see README. Flip these to accepted:true once the fixtures (or the config rules) are reconciled.",
  "happy-path.json": { "accepted": true },
  "boundary-minimum-values.json": {
    "accepted": false,
    "errorContains": "fgSumMin validation failed"
  },
  "complex-eligibility-flags.json": {
    "accepted": false,
    "errorContains": "must have required property 'existingWmps'"
  },
  "large-multi-parcel.json": { "accepted": true },
  "optional-fields-omitted.json": { "accepted": true },
  "decimal-numeric-values.json": { "accepted": true }
}
